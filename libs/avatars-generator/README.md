# avatars-generator

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build avatars-generator` to build the library.

## Running unit tests

Run `nx test avatars-generator` to execute the unit tests via [Jest](https://jestjs.io).
